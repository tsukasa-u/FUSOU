import type { SlotItem, SlotItems } from "../require_info";

export const default_slotitems: SlotItems = {
  slot_items: {},
};

export const default_slotitem: SlotItem = {
  id: 0,
  slotitem_id: 0,
  locked: 0,
  level: 0,
  alv: null,
};
