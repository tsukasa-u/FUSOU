# Cloudflare Pages configuration for FUSOU-WEB
name = "fusou"
compatibility_date = "2024-01-01"
pages_build_output_dir = "dist"

# Durable Objects configuration
[durable_objects]
bindings = [
  { name = "COMPACTOR", class_name = "CompactorDO" }
]
