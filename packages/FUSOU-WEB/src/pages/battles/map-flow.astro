---
export const prerender = false;
const queryParams = Astro.url.search;
const newUrl = `/battles?tab=map-flow${queryParams ? '&' + queryParams.substring(1) : ''}`;
return Astro.redirect(newUrl);
---
