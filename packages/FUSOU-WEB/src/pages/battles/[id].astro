---
export const prerender = false;
const { id } = Astro.params;
const queryParams = Astro.url.search;
const newUrl = `/battles?tab=detail&detail_id=${id}${queryParams ? '&' + queryParams.substring(1) : ''}`;
return Astro.redirect(newUrl);
---
