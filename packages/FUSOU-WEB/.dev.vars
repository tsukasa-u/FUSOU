# Local development overrides for Cloudflare bindings
# These values are injected into the runtime environment via getPlatformProxy()

# R2 custom domain — allows local dev to use production CDN for banner images
ASSET_BASE_URL=https://assets.fusou.dev
